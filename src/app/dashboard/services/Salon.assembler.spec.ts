import {ProviderProfileAssembler} from './ProviderProfileAssembler';

describe('Salons', () => {
  it('should create an instance', () => {
    expect(new ProviderProfileAssembler()).toBeTruthy();
  });
});
